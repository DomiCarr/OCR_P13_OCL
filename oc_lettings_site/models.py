# oc_lettings_site/models.py

"""
Legacy models have been removed.

This module is intentionally kept to avoid import errors if referenced
by older code or migrations. All active models live in their apps:
- lettings.models
- profiles.models
"""
